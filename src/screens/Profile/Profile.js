import React from 'react';
import './Profile.css'

const Profile = (props) => {
    return (
        <div className="Profile-container animated fadeIn">This is profile page</div>
    );
}

export default Profile;